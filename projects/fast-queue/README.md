# fast-queue

A lightweight, high-throughput asynchronous memory queue with persistence and zero dependencies.

## Features
- In-memory ring buffer with disk journaling
- FIFO and priority ordering
- Zero external runtime dependencies
- TypeScript definitions included
